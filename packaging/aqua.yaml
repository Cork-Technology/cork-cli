# Pinned, checksum-verified toolchain for the apk/OCI packaging jobs (aquaproj/aqua-installer
# reads this via AQUA_CONFIG). Versions are exact on purpose — bump deliberately.
---
registries:
  - type: standard
    ref: v4.431.0 # renovate: depName=aquaproj/aqua-registry
packages:
  - name: chainguard-dev/melange@v0.56.4
  - name: chainguard-dev/apko@v1.2.30
checksum:
  enabled: true
  require_checksum: true # aqua-checksums.json committed 2026-08-05 (aqua v2.53.8, all platforms)
