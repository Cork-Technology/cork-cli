# Claim vs Observed — discrepancy table

| # | Claim (source) | Observed (evidence) | Impact on design |
|---|---|---|---|
| D1 | Brief §2: "current Dutch-auction limit-order … prices" are time-dependent computed state | No Dutch-auction extension found in Cork's 1inch LOP v4 usage — orders are plain fixed-price LOP orders (no extension bytes; 1inch `DutchAuctionCalculator` exists upstream but unused). Source: phoenix/lob code + api-phoenix order shape (see research/cork-contracts-domain.md) | If real, the "current Dutch price" tool has nothing to compute — QUESTION(1): is Dutch-auction planned/elsewhere? |
| D2 | Brief §2: rollover-order prices change over time | rollover-private orders are ERC-7683 with a **fixed** `minPremiumPerShare` floor; no on-chain price-over-time curve found | Same tool-surface question — QUESTION(1)/(2) |
| D3 | phoenix `config/prod.toml` mainnet `history_last_deployment_block = 24238826` | Sourcify: WhitelistManager deployed 24134627, CorkAdapter 24134645, PM proxy 24238837 | Backfills must start at 24134627; "last deployment" ≠ "first deployment"; config semantics need labeling |
| D4 | docs.cork.tech one page: MarketId = hash of (collateral, reference) pair | Code + empirical: MarketId = keccak256(abi.encode(8-field Market struct)) — verified by re-hash matching on-chain `getId` | Enrichment layer must cite the *correct* definition; docs page is wrong/stale |
| D5 | docs claim npm package `@cork-technology/phoenix` for ABIs | Package does not exist on npm | ABIs must come from GitHub repo or Sourcify |
| D6 | Domain-agent read of source: previews return zeros when paused/expired | True per source, BUT empty-pool previews return *nonzero real numbers* with no liquidity check; and at vnet head the oracle itself panics (0x11) making all previews revert | Preview outputs need contextual labeling: zero ≠ "cheap", success ≠ executable, revert ≠ pool problem |
| D7 | Brief §4: "Tenderly vnet … verify which time/state RPC methods it accepts" | Not directly tested (avoided mutating shared vnet); instead used forge in-process fork of the vnet for all mutation/warp. vnet head timestamp is ~6 days behind wall clock; vnet-only markets exist (created 2026-07-03-ish) | Time-warp experiments don't need vnet cheatcode RPCs at all — forge fork is cleaner |
| D8 | NatSpec on `withdraw` (per domain agent): pre-expiry withdrawal wording contradiction | Code allows post-expiry withdraw path | Flag in enrichment; ask Cork team which is intended (folded into QUESTION list) |
| D9 | Brief §2: three data modes incl. Lite-Decentralized "free public RPCs from chainlist.org" | publicnode rejects historical getLogs without token; llamarpc down at test time; every archive-ish op needs a fallback pool + clear error taxonomy | Mode design: Lite mode must degrade gracefully & say *why* (no archive) — silent fallback across modes has trust implications (§5.2) |
